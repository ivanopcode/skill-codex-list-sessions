interface:
  display_name: "skill-codex-list-sessions"
  short_description: "Codex sessions, IDs, recent history"
  default_prompt: "Use $skill-codex-list-sessions to list recent Codex sessions, find sessions by topic, recover full session IDs, and summarize the latest user messages."
