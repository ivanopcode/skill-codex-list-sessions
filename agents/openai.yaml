interface:
  display_name: "Codex Sessions"
  short_description: "List recent Codex sessions with full IDs"
  default_prompt: "Use $skill-codex-list-sessions to list recent Codex sessions with full session IDs, overall discussion summaries, and the full last user messages."
